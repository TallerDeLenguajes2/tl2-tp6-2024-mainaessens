@model Producto

<h2 class="text-danger">Eliminar Producto</h2>

<div class="alert alert-danger">
    ¿Está seguro que desea eliminar el siguiente producto?
</div>

<table class="table">
    <tr>
        <th>ID</th>
        <td>@Model.idProducto</td>
    </tr>
    <tr>
        <th>Descripción</th>
        <td>@Model.descripcion</td>
    </tr>
    <tr>
        <th>Precio</th>
        <td>@Model.precio</td>
    </tr>
</table>

<form asp-controller="Productos" asp-action="EliminarProductoConfirmado" method="post">
    <input type="hidden" name="id" value="@Model.IdProducto" />
    <button type="submit" class="btn btn-danger">Confirmar Eliminación</button>
    <a asp-action="ListarProductos" class="btn btn-secondary">Cancelar</a>
</form>